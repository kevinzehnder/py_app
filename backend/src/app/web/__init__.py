"""Server-rendered frontend (Jinja2 + HTMX + Alpine + beercss).

A lightweight alternative to the React SPA for minimal projects: templates are
rendered and served directly by FastAPI, no Node build step. Self-contained —
templates, static assets and the router all live under this package.
"""
